requests>=2.32.0
langgraph>=0.2.0
typing-extensions>=4.10.0
python-dotenv>=1.0.1
# LangSmith — optional but auto-enabled when LANGSMITH_API_KEY is set.
# Provides trace tree + per-LLM-call observability for the reviewer and the
# test generator. Safe to leave installed even with no API key configured.
langsmith>=0.1.83

# Provider SDKs (only the one you pick is needed at runtime, but installing all
# keeps the agent fully pluggable).
anthropic>=0.34.0
openai>=1.40.0
boto3>=1.34.0
